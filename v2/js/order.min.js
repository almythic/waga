(function () { 'use strict'; function isEligibleZip(zip) { var code = parseInt(zip, 10); if (!isNaN(code) && code >= 10001 && code <= 10282) return true; var njZips = ['07302', '07030', '07047']; return njZips.indexOf(zip.trim()) !== -1; } var steps = document.querySelectorAll('.order-step'); var stepIndicator = document.querySelector('.step-indicator'); var stepIndicatorText = document.querySelector('.step-indicator-text'); var stepIndicatorFill = document.querySelector('.step-indicator-fill'); var stepDots = document.querySelectorAll('.step-dot'); var currentStep = 1; var totalSteps = 4; var zipInput = document.getElementById('zip-input'); var zipCheckBtn = document.getElementById('zip-check-btn'); var zipGate = document.getElementById('zip-gate'); var zipWaitlist = document.getElementById('zip-waitlist'); var zipWaitlistConfirmed = document.getElementById('zip-waitlist-confirmed'); function checkZip() { if (!zipInput) return; var zip = zipInput.value.trim(); if (!zip || zip.length < 5) { zipInput.classList.add('error'); return; } if (isEligibleZip(zip)) { var addressZipField = document.getElementById('address-zip'); if (addressZipField) addressZipField.value = zip; showStep(2); } else { var waitlistZipField = document.getElementById('waitlist-zip'); if (waitlistZipField) waitlistZipField.value = zip; zipGate.hidden = true; zipWaitlist.hidden = false; if (stepIndicator) stepIndicator.classList.add('hidden'); } } if (zipCheckBtn) { zipCheckBtn.addEventListener('click', checkZip); } if (zipInput) { zipInput.addEventListener('keydown', function (e) { if (e.key === 'Enter') { e.preventDefault(); checkZip(); } }); zipInput.addEventListener('input', function () { this.classList.remove('error'); this.value = this.value.replace(/\D/g, '').slice(0, 5); }); } var waitlistBackBtn = document.getElementById('waitlist-back-btn'); if (waitlistBackBtn) { waitlistBackBtn.addEventListener('click', function () { zipGate.hidden = false; zipWaitlist.hidden = true; if (stepIndicator) stepIndicator.classList.remove('hidden'); if (zipInput) { zipInput.value = ''; zipInput.focus(); } }); } var waitlistSubmitBtn = document.getElementById('waitlist-submit-btn'); if (waitlistSubmitBtn) { waitlistSubmitBtn.addEventListener('click', function () { var nameEl = document.getElementById('waitlist-name'); var emailEl = document.getElementById('waitlist-email'); var name = nameEl ? nameEl.value.trim() : ''; var email = emailEl ? emailEl.value.trim() : ''; var valid = true; if (!name) { showFieldError(nameEl, 'Please enter your name'); valid = false; } if (!email || !isValidEmail(email)) { showFieldError(emailEl, !email ? 'Please enter your email' : 'Please enter a valid email'); valid = false; } if (!valid) return; zipWaitlist.hidden = true; zipWaitlistConfirmed.hidden = false; }); } ['waitlist-name', 'waitlist-email'].forEach(function (id) { var el = document.getElementById(id); if (el) { el.addEventListener('input', function () { this.classList.remove('error'); }); } }); function scrollToTop() { if (window.__lenis) { window.__lenis.scrollTo(0, { immediate: true }); } else { window.scrollTo({ top: 0, behavior: 'instant' }); } } function showStep(n) { steps.forEach(function (step) { step.classList.remove('active'); }); var target = document.getElementById('step-' + n); if (target) { target.classList.add('active'); currentStep = n; updateIndicator(); scrollToTop(); } if (n === 3) updateOrderSummaryDate(); if (n === 5) updateConfirmationDate(); } function updateIndicator() { if (currentStep >= 5) { if (stepIndicator) stepIndicator.classList.add('hidden'); return; } if (stepIndicator) stepIndicator.classList.remove('hidden'); if (stepIndicatorText) { stepIndicatorText.textContent = 'Step ' + currentStep + ' of ' + totalSteps; } if (stepIndicatorFill) { stepIndicatorFill.style.width = ((currentStep / totalSteps) * 100) + '%'; } stepDots.forEach(function (dot, index) { dot.classList.remove('active', 'completed'); if (index + 1 === currentStep) { dot.classList.add('active'); } else if (index + 1 < currentStep) { dot.classList.add('completed'); } }); } function goNext() { if (!validateStep(currentStep)) return; if (currentStep < totalSteps) { showStep(currentStep + 1); } else { goToConfirmation(); } } function goBack() { if (currentStep <= 1) return; showStep(currentStep - 1); } function goToConfirmation() { steps.forEach(function (step) { step.classList.remove('active'); }); var confirmation = document.getElementById('step-5'); if (confirmation) { confirmation.classList.add('active'); currentStep = 5; updateIndicator(); updateConfirmationDate(); scrollToTop(); } } document.querySelectorAll('[data-action="next"]').forEach(function (btn) { btn.addEventListener('click', goNext); }); document.querySelectorAll('[data-action="back"]').forEach(function (btn) { btn.addEventListener('click', goBack); }); var skipProfileBtn = document.getElementById('skip-profile-btn'); if (skipProfileBtn) { skipProfileBtn.addEventListener('click', goToConfirmation); } var orderForm = document.getElementById('order-form'); if (orderForm) { orderForm.addEventListener('submit', function (e) { e.preventDefault(); }); } var selectableDateCards = document.querySelectorAll('.date-card:not(.sold-out)'); var selectedDateInput = document.getElementById('selected-date'); selectableDateCards.forEach(function (card) { card.addEventListener('click', function () { selectableDateCards.forEach(function (c) { c.classList.remove('selected'); c.setAttribute('aria-checked', 'false'); }); card.classList.add('selected'); card.setAttribute('aria-checked', 'true'); if (selectedDateInput) selectedDateInput.value = card.getAttribute('data-date'); var dateError = document.getElementById('date-error'); if (dateError) dateError.classList.remove('visible'); }); card.addEventListener('keydown', function (e) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); card.click(); } }); }); function formatSelectedDate() { if (!selectedDateInput || !selectedDateInput.value) return null; var d = new Date(selectedDateInput.value + 'T00:00:00'); return d.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }); } function updateOrderSummaryDate() { var summaryDate = document.getElementById('summary-selected-date'); if (!summaryDate) return; summaryDate.textContent = formatSelectedDate() || '—'; } function updateConfirmationDate() { var confirmDateEl = document.getElementById('confirmation-date-display'); if (!confirmDateEl) return; var formatted = formatSelectedDate(); confirmDateEl.textContent = formatted ? 'Starting ' + formatted : ''; } document.querySelectorAll('.form-chip-group').forEach(function (group) { var fieldId = group.getAttribute('data-field'); var hiddenInput = document.getElementById(fieldId); var isSingle = group.getAttribute('data-single') === 'true'; function syncHidden() { var selected = []; group.querySelectorAll('.form-chip.selected').forEach(function (chip) { selected.push(chip.getAttribute('data-value')); }); if (hiddenInput) hiddenInput.value = selected.join(', '); } function toggleOtherField(show) { var otherField = document.getElementById(fieldId + '-other-field'); if (otherField) otherField.hidden = !show; } group.querySelectorAll('.form-chip').forEach(function (chip) { chip.addEventListener('click', function () { var val = chip.getAttribute('data-value'); if (isSingle) { group.querySelectorAll('.form-chip').forEach(function (c) { c.classList.remove('selected'); }); chip.classList.add('selected'); var fieldError = document.getElementById(fieldId + '-error'); if (fieldError) fieldError.classList.remove('visible'); } else if (val === 'None') { group.querySelectorAll('.form-chip').forEach(function (c) { c.classList.remove('selected'); }); chip.classList.add('selected'); toggleOtherField(false); } else { var noneChip = group.querySelector('.form-chip[data-value="None"]'); if (noneChip) noneChip.classList.remove('selected'); chip.classList.toggle('selected'); var otherChip = group.querySelector('.form-chip[data-value="Other"]'); toggleOtherField(otherChip && otherChip.classList.contains('selected')); } syncHidden(); }); }); }); var visaLogo = document.getElementById('card-logo-visa'); var mcLogo = document.getElementById('card-logo-mc'); function detectCardType(digits) { if (/^4/.test(digits)) return 'visa'; if (/^(5[1-5]|2[2-7]\d{2})/.test(digits)) return 'mastercard'; return null; } function updateCardLogos(digits) { var type = detectCardType(digits); if (visaLogo) visaLogo.hidden = (type !== 'visa'); if (mcLogo) mcLogo.hidden = (type !== 'mastercard'); } var cardNumberInput = document.getElementById('card-number'); if (cardNumberInput) { cardNumberInput.addEventListener('input', function () { var val = this.value.replace(/\D/g, '').slice(0, 16); var groups = val.match(/.{1,4}/g); this.value = groups ? groups.join(' ') : val; updateCardLogos(val); }); } var cardExpiryInput = document.getElementById('card-expiry'); if (cardExpiryInput) { cardExpiryInput.addEventListener('input', function () { var val = this.value.replace(/\D/g, '').slice(0, 4); this.value = val.length >= 3 ? val.slice(0, 2) + ' / ' + val.slice(2) : val; }); } var cardCvvInput = document.getElementById('card-cvv'); if (cardCvvInput) { cardCvvInput.addEventListener('input', function () { this.value = this.value.replace(/\D/g, '').slice(0, 4); }); } function isValidEmail(email) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email); } function validateStep(step) { if (step === 1) return true; var stepEl = document.getElementById('step-' + step); if (!stepEl) return true; var valid = true; stepEl.querySelectorAll('[required]').forEach(function (field) { clearFieldError(field); if (!field.value.trim()) { showFieldError(field, 'This field is required'); valid = false; } else if (field.type === 'email' && !isValidEmail(field.value)) { showFieldError(field, 'Please enter a valid email'); valid = false; } }); if (step === 2) { if (!selectedDateInput || !selectedDateInput.value) { var dateError = document.getElementById('date-error'); if (dateError) dateError.classList.add('visible'); valid = false; } } if (step === 3) { var termsCheckbox = document.getElementById('terms-agree'); var termsError = document.getElementById('terms-error'); if (termsCheckbox && !termsCheckbox.checked) { if (termsError) termsError.classList.add('visible'); valid = false; } } if (step === 4) { var dietInput = document.getElementById('diet'); if (!dietInput || !dietInput.value) { var dietError = document.getElementById('diet-error'); if (dietError) dietError.classList.add('visible'); valid = false; } } if (!valid) { var firstError = stepEl.querySelector('.form-input.error, .form-error-message.visible'); if (firstError) { firstError.scrollIntoView({ behavior: 'smooth', block: 'center' }); } } return valid; } function showFieldError(field, message) { field.classList.add('error'); var group = field.closest('.form-group'); if (group) { var errorEl = group.querySelector('.form-error-message'); if (errorEl) { errorEl.textContent = message; errorEl.classList.add('visible'); } } } function clearFieldError(field) { field.classList.remove('error'); var group = field.closest('.form-group'); if (group) { var msg = group.querySelector('.form-error-message'); if (msg) msg.classList.remove('visible'); } } document.querySelectorAll('.form-input, .form-textarea').forEach(function (field) { field.addEventListener('input', function () { if (!this.readOnly) clearFieldError(this); }); }); var faqItems = document.querySelectorAll('.faq-item'); faqItems.forEach(function (item) { var question = item.querySelector('.faq-question'); var answer = item.querySelector('.faq-answer'); if (!question || !answer) return; question.addEventListener('click', function () { var isOpen = item.classList.contains('open'); faqItems.forEach(function (other) { if (other !== item) { other.classList.remove('open'); var otherAnswer = other.querySelector('.faq-answer'); if (otherAnswer) otherAnswer.style.maxHeight = null; var otherQ = other.querySelector('.faq-question'); if (otherQ) otherQ.setAttribute('aria-expanded', 'false'); } }); if (isOpen) { item.classList.remove('open'); answer.style.maxHeight = null; question.setAttribute('aria-expanded', 'false'); } else { item.classList.add('open'); answer.style.maxHeight = answer.scrollHeight + 'px'; question.setAttribute('aria-expanded', 'true'); } }); question.addEventListener('keydown', function (e) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); question.click(); } }); }); var termsChk = document.getElementById('terms-agree'); if (termsChk) { termsChk.addEventListener('change', function () { if (this.checked) { var te = document.getElementById('terms-error'); if (te) te.classList.remove('visible'); } }); } showStep(1); })();